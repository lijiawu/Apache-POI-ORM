package POI.UseCase;

public class Carcassonne {

//    public void get(String filePath) {
//        NewXlsx xlsx = Xlsx.load(filePath); // FIXME
//        NewSheet<Player> playerSheet = xlsx.getSheet(Player.class);
//
//        // 1. Want to get start from 2
//        List<Player> list = playerSheet.get(new Range(2, 100));
//
//        // 2. First might fail
//        list = playerSheet.getAll();
//
//        // 3. iterate?
//        Player player;
//        int row = 2;
//        do {
//            player = playerSheet.get(row); // get (int) returns list
//            // FIXME: if no error
//            list.add(player);
//            row++;
//        } while (true);
//
//    }
//
//    public void add(String filePath, List<Player> players, List<Tile> tiles) {
//        NewXlsx xlsx = Xlsx.load(filePath); // Can we rewrite?
//        NewSheet<Player> playerSheet = xlsx.createSheet(Player.class);
//
//        playerSheet.addAll(players, 2);
//    }
}
